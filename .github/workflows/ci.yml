name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  installer:
    name: Test installer
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run installer tests
        run: scripts/install.test.sh
